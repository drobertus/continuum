apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {

    compile(
        localGroovy()
    )

    testCompile(
        group: 'junit', name: 'junit', version: '4.11',

    )
}